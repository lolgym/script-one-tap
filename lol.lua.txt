-- ALL IN ONE (FINAL FIXED)
-- X = Menu | Z = AIM | F = FLY

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

local ESPon = true
local ESPcolor = Color3.fromRGB(255, 50, 50)
local ESPsize = 14
local BoxSize = 3.5

local AIMon = false
local Smoothness = 0.3
local FOV = 200
local CheckWalls = true
local ShowFOVCircle = true

local Fly = false
local NoClip = false
local Speed = 1

local menuOpen = true
local gui = nil
local fovCircle = nil
local mobileButton = nil
local scriptLoaded = true
local currentTab = "COMBAT"
local updateConn = nil
local flyConn = nil
local respawning = false
local dragging = false
local dragStart = nil
local startPos = nil

local CENTER_OFFSET = Vector2.new(0, -57)

-- ========== ЭКРАН ЗАГРУЗКИ ==========
local function ShowLoadingScreen()
    local screenGui = Instance.new("ScreenGui")
    screenGui.Parent = LocalPlayer.PlayerGui
    screenGui.Name = "LoadingScreen"
    screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    screenGui.ResetOnSpawn = false
    screenGui.DisplayOrder = 999
    
    local background = Instance.new("Frame")
    background.Size = UDim2.new(1, 0, 1, 0)
    background.BackgroundColor3 = Color3.fromRGB(8, 8, 16)
    background.BackgroundTransparency = 0
    background.ZIndex = 999
    background.Parent = screenGui
    
    local glow = Instance.new("Frame")
    glow.Size = UDim2.new(0, 300, 0, 300)
    glow.Position = UDim2.new(0.5, -150, 0.5, -160)
    glow.BackgroundColor3 = Color3.fromRGB(255, 30, 30)
    glow.BackgroundTransparency = 0.85
    glow.BorderSizePixel = 0
    glow.ZIndex = 1000
    glow.Parent = background
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 150)
    corner.Parent = glow
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(0, 0, 0, 70)
    title.Position = UDim2.new(0.5, 0, 0.5, -70)
    title.BackgroundTransparency = 1
    title.Text = "ALL IN ONE"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextSize = 52
    title.Font = Enum.Font.GothamBold
    title.TextScaled = false
    title.ZIndex = 1000
    title.Parent = background
    
    local subtitle = Instance.new("TextLabel")
    subtitle.Size = UDim2.new(0, 0, 0, 30)
    subtitle.Position = UDim2.new(0.5, 0, 0.5, 10)
    subtitle.BackgroundTransparency = 1
    subtitle.Text = "Loading"
    subtitle.TextColor3 = Color3.fromRGB(180, 180, 200)
    subtitle.TextSize = 20
    subtitle.Font = Enum.Font.Gotham
    subtitle.TextScaled = false
    subtitle.ZIndex = 1000
    subtitle.Parent = background
    
    local dotsLabel = Instance.new("TextLabel")
    dotsLabel.Size = UDim2.new(0, 40, 0, 30)
    dotsLabel.Position = UDim2.new(0.5, 30, 0.5, 10)
    dotsLabel.BackgroundTransparency = 1
    dotsLabel.Text = ""
    dotsLabel.TextColor3 = Color3.fromRGB(180, 180, 200)
    dotsLabel.TextSize = 20
    dotsLabel.Font = Enum.Font.Gotham
    dotsLabel.TextScaled = false
    dotsLabel.TextXAlignment = Enum.TextXAlignment.Left
    dotsLabel.ZIndex = 1000
    dotsLabel.Parent = background
    
    local barBg = Instance.new("Frame")
    barBg.Size = UDim2.new(0, 220, 0, 3)
    barBg.Position = UDim2.new(0.5, -110, 0.5, 45)
    barBg.BackgroundColor3 = Color3.fromRGB(40, 40, 55)
    barBg.BorderSizePixel = 0
    barBg.ZIndex = 1000
    barBg.Parent = background
    
    local bar = Instance.new("Frame")
    bar.Size = UDim2.new(0, 0, 1, 0)
    bar.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
    bar.BorderSizePixel = 0
    bar.ZIndex = 1001
    bar.Parent = barBg
    
    local dotCount = 0
    local dotConnection = RunService.Heartbeat:Connect(function()
        if not screenGui or not screenGui.Parent then
            dotConnection:Disconnect()
            return
        end
        dotCount = (dotCount + 1) % 4
        dotsLabel.Text = string.rep(".", dotCount)
    end)
    
    local barTween = TweenService:Create(bar, TweenInfo.new(5, Enum.EasingStyle.Linear), {Size = UDim2.new(1, 0, 1, 0)})
    barTween:Play()
    
    task.wait(5)
    dotConnection:Disconnect()
    
    local fadeOut = TweenService:Create(background, TweenInfo.new(0.5, Enum.EasingStyle.Linear), {BackgroundTransparency = 1})
    local glowFade = TweenService:Create(glow, TweenInfo.new(0.5, Enum.EasingStyle.Linear), {BackgroundTransparency = 1})
    local titleFade = TweenService:Create(title, TweenInfo.new(0.5, Enum.EasingStyle.Linear), {TextTransparency = 1})
    local subFade = TweenService:Create(subtitle, TweenInfo.new(0.5, Enum.EasingStyle.Linear), {TextTransparency = 1})
    local dotsFade = TweenService:Create(dotsLabel, TweenInfo.new(0.5, Enum.EasingStyle.Linear), {TextTransparency = 1})
    local barFade = TweenService:Create(barBg, TweenInfo.new(0.5, Enum.EasingStyle.Linear), {BackgroundTransparency = 1})
    
    fadeOut:Play()
    glowFade:Play()
    titleFade:Play()
    subFade:Play()
    dotsFade:Play()
    barFade:Play()
    
    task.wait(0.6)
    screenGui:Destroy()
    print("Loading complete")
end

-- UNLOAD
local function Unload()
    scriptLoaded = false
    if fovCircle then fovCircle:Destroy() fovCircle = nil end
    if gui then gui:Destroy() gui = nil end
    if mobileButton then mobileButton:Destroy() mobileButton = nil end
    if updateConn then updateConn:Disconnect() end
    if flyConn then flyConn:Disconnect() end
    
    local char = LocalPlayer.Character
    if char then
        local hum = char:FindFirstChild("Humanoid")
        if hum then
            hum.PlatformStand = false
            hum.Jump = true
        end
    end
    
    for _, p in ipairs(Players:GetPlayers()) do
        if p.Character then
            for _, o in ipairs(p.Character:GetChildren()) do
                if o:IsA("BoxHandleAdornment") or o:IsA("BillboardGui") then
                    o:Destroy()
                end
            end
        end
    end
    for _, c in ipairs(workspace:GetChildren()) do
        if c:IsA("Model") and c:FindFirstChild("Humanoid") then
            for _, o in ipairs(c:GetChildren()) do
                if o:IsA("BoxHandleAdornment") or o:IsA("BillboardGui") then
                    o:Destroy()
                end
            end
        end
    end
    print("Unloaded")
end

-- CENTER
local function GetCenter()
    local size = Camera.ViewportSize or Vector2.new(1920, 1080)
    return Vector2.new(size.X/2 + CENTER_OFFSET.X, size.Y/2 + CENTER_OFFSET.Y)
end

-- FOV CIRCLE
local function CreateFOVCircle()
    if fovCircle then fovCircle:Destroy() fovCircle = nil end
    local center = GetCenter()
    local r = FOV
    local c = Instance.new("ScreenGui")
    c.Parent = LocalPlayer.PlayerGui
    c.ResetOnSpawn = false
    c.Name = "FOVCircle"
    c.Enabled = false
    
    local f = Instance.new("Frame")
    f.Size = UDim2.new(0, r*2, 0, r*2)
    f.Position = UDim2.new(0, center.X - r, 0, center.Y - r)
    f.BackgroundTransparency = 1
    f.Parent = c
    f.Name = "CircleFrame"
    
    for i = 0, 63 do
        local a = (i/64) * 2 * math.pi
        local na = ((i+1)/64) * 2 * math.pi
        local x1, y1 = r*math.cos(a), r*math.sin(a)
        local x2, y2 = r*math.cos(na), r*math.sin(na)
        local mx, my = (x1+x2)/2, (y1+y2)/2
        local len = math.sqrt((x2-x1)^2 + (y2-y1)^2)
        local rot = math.atan2(y2-y1, x2-x1) * 180 / math.pi
        
        local seg = Instance.new("Frame")
        seg.Size = UDim2.new(0, len, 0, 2)
        seg.Position = UDim2.new(0.5, mx - len/2, 0.5, my - 1)
        seg.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
        seg.BackgroundTransparency = 0.4
        seg.BorderSizePixel = 0
        seg.Rotation = rot
        seg.Parent = f
    end
    
    local dot = Instance.new("Frame")
    dot.Size = UDim2.new(0, 3, 0, 3)
    dot.Position = UDim2.new(0.5, -1.5, 0.5, -1.5)
    dot.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
    dot.BackgroundTransparency = 0.2
    dot.BorderSizePixel = 0
    dot.Parent = f
    
    fovCircle = c
    return c
end

local function UpdateFOVCircle()
    if not fovCircle then return end
    local f = fovCircle:FindFirstChild("CircleFrame")
    if not f then return end
    local center = GetCenter()
    local r = FOV
    f.Size = UDim2.new(0, r*2, 0, r*2)
    f.Position = UDim2.new(0, center.X - r, 0, center.Y - r)
    local children = f:GetChildren()
    for i = 0, 63 do
        local a = (i/64) * 2 * math.pi
        local na = ((i+1)/64) * 2 * math.pi
        local x1, y1 = r*math.cos(a), r*math.sin(a)
        local x2, y2 = r*math.cos(na), r*math.sin(na)
        local mx, my = (x1+x2)/2, (y1+y2)/2
        local len = math.sqrt((x2-x1)^2 + (y2-y1)^2)
        local rot = math.atan2(y2-y1, x2-x1) * 180 / math.pi
        local seg = children[i+1]
        if seg and seg:IsA("Frame") then
            seg.Size = UDim2.new(0, len, 0, 2)
            seg.Position = UDim2.new(0.5, mx - len/2, 0.5, my - 1)
            seg.Rotation = rot
        end
    end
end

local function ToggleFOVCircle(show)
    if fovCircle then
        fovCircle.Enabled = show and ShowFOVCircle
    end
end

-- NPC
local function IsNPC(c)
    if not c then return false end
    for _, p in ipairs(Players:GetPlayers()) do
        if p.Character == c then return false end
    end
    return true
end

local function GetName(c)
    if IsNPC(c) then return "NPC" end
    for _, p in ipairs(Players:GetPlayers()) do
        if p.Character == c then return p.Name end
    end
    return "?"
end

local function IsVisible(c)
    if not CheckWalls then return true end
    local r = c:FindFirstChild("HumanoidRootPart")
    if not r then return false end
    local o = Camera.CFrame.Position
    local t = r.Position + Vector3.new(0, 1.5, 0)
    local ray = Ray.new(o, (t - o).Unit * (t - o).Magnitude)
    local h = workspace:FindPartOnRay(ray, LocalPlayer.Character)
    if h then
        local p = h.Parent
        if p == c or p:IsDescendantOf(c) then return true end
        return false
    end
    return true
end

local function GetTargets()
    local t = {}
    for _, p in ipairs(Players:GetPlayers()) do
        local c = p.Character
        if c and c ~= LocalPlayer.Character and c:FindFirstChild("HumanoidRootPart") and c:FindFirstChild("Humanoid") and c.Humanoid.Health > 0 then
            table.insert(t, c)
        end
    end
    for _, c in ipairs(workspace:GetChildren()) do
        if c:IsA("Model") and c:FindFirstChild("Humanoid") and c:FindFirstChild("HumanoidRootPart") and c ~= LocalPlayer.Character then
            local isP = false
            for _, p in ipairs(Players:GetPlayers()) do
                if p.Character == c then isP = true break end
            end
            if not isP and c.Humanoid.Health > 0 then
                table.insert(t, c)
            end
        end
    end
    return t
end

local function GetClosest()
    local targets = GetTargets()
    local closest = nil
    local dist = math.huge
    local center = Vector2.new(Mouse.X, Mouse.Y)
    for _, t in ipairs(targets) do
        local r = t:FindFirstChild("HumanoidRootPart")
        if r then
            local pos, on = Camera:WorldToScreenPoint(r.Position)
            if on then
                local d = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                if d < dist and d < FOV and IsVisible(t) then
                    dist = d
                    closest = t
                end
            end
        end
    end
    return closest
end

local function CreateESP(c)
    if not c or not c:FindFirstChild("HumanoidRootPart") or c == LocalPlayer.Character then return end
    local box = Instance.new("BoxHandleAdornment")
    box.Adornee = c
    box.Size = Vector3.new(BoxSize, BoxSize + 1.5, 1)
    box.Color3 = ESPcolor
    box.Transparency = 0.4
    box.AlwaysOnTop = true
    box.ZIndex = 10
    box.Parent = c
    
    local tag = Instance.new("BillboardGui")
    tag.Adornee = c:FindFirstChild("Head") or c:FindFirstChild("HumanoidRootPart")
    tag.Size = UDim2.new(0, 150, 0, 30)
    tag.AlwaysOnTop = true
    tag.Parent = c
    
    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = GetName(c)
    label.TextColor3 = Color3.fromRGB(255, 255, 255)
    label.TextSize = ESPsize
    label.Font = Enum.Font.GothamBold
    label.Parent = tag
end

local function RemoveESP(c)
    if not c then return end
    for _, o in ipairs(c:GetChildren()) do
        if o:IsA("BoxHandleAdornment") or o:IsA("BillboardGui") then
            o:Destroy()
        end
    end
end

-- FLY
local function StartFlyLoop()
    if flyConn then flyConn:Disconnect() end
    flyConn = RunService.Heartbeat:Connect(function()
        if not scriptLoaded or not Fly then return end
        local char = LocalPlayer.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        
        local move = Vector3.new(0, 0, 0)
        local spd = 50 * Speed
        
        if UserInputService:IsKeyDown(Enum.KeyCode.W) then move = move + Camera.CFrame.LookVector * spd end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then move = move - Camera.CFrame.LookVector * spd end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then move = move - Camera.CFrame.RightVector * spd end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then move = move + Camera.CFrame.RightVector * spd end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then move = move + Vector3.new(0, spd, 0) end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then move = move - Vector3.new(0, spd, 0) end
        
        if move.Magnitude > 0 then
            hrp.CFrame = hrp.CFrame + move * 0.06
        end
        
        local hum = char:FindFirstChild("Humanoid")
        if hum then hum.PlatformStand = true end
    end)
end

local function StartUpdateLoop()
    if updateConn then updateConn:Disconnect() end
    updateConn = RunService.Heartbeat:Connect(function()
        if not scriptLoaded then return end
        local char = LocalPlayer.Character
        if not char then return end
        
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChild("Humanoid")
        if not hrp or not hum then return end
        
        if NoClip then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end
        end
        
        hum.WalkSpeed = 16 * Speed
        hum.JumpPower = 50 * Speed
        
        if Fly then
            hum.PlatformStand = true
            hrp.Anchored = false
        else
            hum.PlatformStand = false
            hrp.Anchored = false
        end
    end)
end

-- RESPAWN
local function ForceRespawn()
    if respawning then return end
    respawning = true
    
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local pos = hrp and hrp.Position or Vector3.new(0, 10, 0)
        
        local hum = char:FindFirstChild("Humanoid")
        if hum then
            hum.Health = 0
        end
        
        task.wait(0.5)
        
        local newChar = LocalPlayer.Character
        if newChar then
            local newHrp = newChar:FindFirstChild("HumanoidRootPart")
            if newHrp then
                newHrp.CFrame = CFrame.new(pos)
            end
            local newHum = newChar:FindFirstChild("Humanoid")
            if newHum then
                newHum.WalkSpeed = 16 * Speed
                newHum.JumpPower = 50 * Speed
                newHum.Jump = true
                newHum.PlatformStand = false
            end
        end
    end
    
    respawning = false
end

local function ToggleFly()
    Fly = not Fly
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChild("Humanoid")
    
    if Fly then
        StartFlyLoop()
        if hum then 
            hum.PlatformStand = true
            hum.Jump = true
        end
        print("FLY ON")
    else
        if flyConn then flyConn:Disconnect() flyConn = nil end
        if hum then 
            hum.PlatformStand = false
            hum.WalkSpeed = 16 * Speed
            hum.JumpPower = 50 * Speed
            hum.Jump = true
        end
        print("FLY OFF - Respawn...")
        ForceRespawn()
    end
end

local function ToggleNoClip()
    NoClip = not NoClip
end

local function SetSpeed(v)
    Speed = v
end

-- MOBILE BUTTON
local function CreateMobileButton()
    if mobileButton then mobileButton:Destroy() mobileButton = nil end
    local btnGui = Instance.new("ScreenGui")
    btnGui.Parent = LocalPlayer.PlayerGui
    btnGui.ResetOnSpawn = false
    btnGui.Name = "MobileButton"
    
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 60, 0, 60)
    btn.Position = UDim2.new(1, -75, 0, 10)
    btn.BackgroundColor3 = Color3.fromRGB(255, 40, 40)
    btn.BackgroundTransparency = 0.2
    btn.BorderSizePixel = 2
    btn.BorderColor3 = Color3.fromRGB(255, 255, 255)
    btn.Text = "MENU"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 14
    btn.Font = Enum.Font.GothamBold
    btn.Parent = btnGui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 30)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        menuOpen = not menuOpen
        if gui then
            local m = gui:FindFirstChild("Main")
            if m then m.Visible = menuOpen end
        end
    end)
    
    mobileButton = btnGui
    return btnGui
end

-- ========== МЕНЮ С ПЕРЕТАСКИВАНИЕМ (ФИКС) ==========
local function CreateMenu()
    if gui then gui:Destroy() gui = nil end
    
    gui = Instance.new("ScreenGui")
    gui.Parent = LocalPlayer.PlayerGui
    gui.ResetOnSpawn = false
    gui.Name = "MainMenu"
    gui.Enabled = true
    
    local main = Instance.new("Frame")
    main.Size = UDim2.new(0, 300, 0, 380)
    main.Position = UDim2.new(0.5, -150, 0.5, -190)
    main.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    main.BackgroundTransparency = 0.1
    main.BorderSizePixel = 1
    main.BorderColor3 = Color3.fromRGB(255, 40, 40)
    main.Parent = gui
    main.Visible = true  -- ПОКАЗЫВАЕМ СРАЗУ
    main.Name = "Main"
    
    -- Header
    local h = Instance.new("Frame")
    h.Size = UDim2.new(1, 0, 0, 30)
    h.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    h.BorderSizePixel = 0
    h.Parent = main
    h.Name = "Header"
    
    -- Заголовок
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(0.7, 0, 1, 0)
    title.Position = UDim2.new(0.05, 0, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "ALL IN ONE"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.TextSize = 16
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Parent = h
    
    -- Кнопка закрытия
    local close = Instance.new("TextButton")
    close.Size = UDim2.new(0, 24, 0, 24)
    close.Position = UDim2.new(0.92, 0, 0.03, 0)
    close.BackgroundTransparency = 1
    close.Text = "✕"
    close.TextColor3 = Color3.fromRGB(200, 200, 200)
    close.TextSize = 14
    close.Font = Enum.Font.GothamBold
    close.Parent = h
    close.MouseButton1Click:Connect(function()
        menuOpen = false
        main.Visible = false
    end)
    
    -- === ПЕРЕТАСКИВАНИЕ МЕНЮ (ИСПРАВЛЕНО) ===
    h.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = main.Position
        end
    end)
    
    h.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
            local delta = input.Position - dragStart
            local newPos = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
            main.Position = newPos
        end
    end)
    
    -- Tabs
    local tb = Instance.new("Frame")
    tb.Size = UDim2.new(1, 0, 0, 26)
    tb.Position = UDim2.new(0, 0, 0, 30)
    tb.BackgroundColor3 = Color3.fromRGB(15, 15, 22)
    tb.BorderSizePixel = 0
    tb.Parent = main
    
    local content = Instance.new("Frame")
    content.Size = UDim2.new(0.9, 0, 0.6, 0)
    content.Position = UDim2.new(0.05, 0, 0.25, 0)
    content.BackgroundColor3 = Color3.fromRGB(25, 25, 38)
    content.Parent = main
    content.Name = "Content"
    
    local unload = Instance.new("TextButton")
    unload.Size = UDim2.new(0.8, 0, 0, 24)
    unload.Position = UDim2.new(0.1, 0, 0.91, 0)
    unload.BackgroundColor3 = Color3.fromRGB(160, 20, 20)
    unload.BackgroundTransparency = 0.3
    unload.TextColor3 = Color3.fromRGB(255, 200, 200)
    unload.TextSize = 12
    unload.Font = Enum.Font.GothamBold
    unload.Text = "UNLOAD"
    unload.Parent = main
    unload.MouseButton1Click:Connect(Unload)
    
    local function MakeToggle(parent, label, y, getter, setter)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.95, 0, 0, 24)
        btn.Position = UDim2.new(0.025, 0, y, 0)
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
        btn.BorderSizePixel = 0
        btn.Text = ""
        btn.Parent = parent
        
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.6, 0, 1, 0)
        lbl.Position = UDim2.new(0.05, 0, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = label
        lbl.TextColor3 = Color3.fromRGB(220, 220, 230)
        lbl.TextSize = 12
        lbl.Font = Enum.Font.Gotham
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = btn
        
        local st = Instance.new("TextLabel")
        st.Size = UDim2.new(0.2, 0, 1, 0)
        st.Position = UDim2.new(0.75, 0, 0, 0)
        st.BackgroundTransparency = 1
        st.Text = getter() and "ON" or "OFF"
        st.TextColor3 = getter() and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 80, 80)
        st.TextSize = 11
        st.Font = Enum.Font.GothamBold
        st.TextXAlignment = Enum.TextXAlignment.Right
        st.Parent = btn
        
        btn.MouseButton1Click:Connect(function()
            setter(not getter())
            st.Text = getter() and "ON" or "OFF"
            st.TextColor3 = getter() and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 80, 80)
        end)
        return btn
    end
    
    local function MakeSlider(parent, label, y, min, max, getter, setter)
        local c = Instance.new("Frame")
        c.Size = UDim2.new(0.95, 0, 0, 36)
        c.Position = UDim2.new(0.025, 0, y, 0)
        c.BackgroundTransparency = 1
        c.Parent = parent
        
        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.5, 0, 0, 14)
        lbl.Position = UDim2.new(0, 0, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = label
        lbl.TextColor3 = Color3.fromRGB(220, 220, 230)
        lbl.TextSize = 12
        lbl.Font = Enum.Font.Gotham
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        lbl.Parent = c
        
        local valLbl = Instance.new("TextLabel")
        valLbl.Size = UDim2.new(0.3, 0, 0, 14)
        valLbl.Position = UDim2.new(0.65, 0, 0, 0)
        valLbl.BackgroundTransparency = 1
        valLbl.TextColor3 = Color3.fromRGB(0, 255, 200)
        valLbl.TextSize = 12
        valLbl.Font = Enum.Font.GothamBold
        valLbl.Text = tostring(getter())
        valLbl.TextXAlignment = Enum.TextXAlignment.Right
        valLbl.Parent = c
        
        local barBg = Instance.new("Frame")
        barBg.Size = UDim2.new(0.7, 0, 0, 2)
        barBg.Position = UDim2.new(0, 0, 0.7, 0)
        barBg.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        barBg.BorderSizePixel = 0
        barBg.Parent = c
        
        local bar = Instance.new("Frame")
        bar.Size = UDim2.new((getter() - min) / (max - min), 0, 1, 0)
        bar.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
        bar.BorderSizePixel = 0
        bar.Parent = barBg
        
        local minus = Instance.new("TextButton")
        minus.Size = UDim2.new(0, 16, 0, 16)
        minus.Position = UDim2.new(0.73, 0, 0.5, 0)
        minus.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        minus.BackgroundTransparency = 0.3
        minus.TextColor3 = Color3.fromRGB(200, 200, 200)
        minus.TextSize = 12
        minus.Font = Enum.Font.GothamBold
        minus.Text = "−"
        minus.Parent = c
        minus.MouseButton1Click:Connect(function()
            local step = (max - min) > 50 and 5 or 1
            local v = math.max(min, getter() - step)
            setter(v)
            valLbl.Text = tostring(v)
            bar.Size = UDim2.new((v - min) / (max - min), 0, 1, 0)
        end)
        
        local plus = Instance.new("TextButton")
        plus.Size = UDim2.new(0, 16, 0, 16)
        plus.Position = UDim2.new(0.84, 0, 0.5, 0)
        plus.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
        plus.BackgroundTransparency = 0.3
        plus.TextColor3 = Color3.fromRGB(200, 200, 200)
        plus.TextSize = 12
        plus.Font = Enum.Font.GothamBold
        plus.Text = "+"
        plus.Parent = c
        plus.MouseButton1Click:Connect(function()
            local step = (max - min) > 50 and 5 or 1
            local v = math.min(max, getter() + step)
            setter(v)
            valLbl.Text = tostring(v)
            bar.Size = UDim2.new((v - min) / (max - min), 0, 1, 0)
        end)
        return c
    end
    
    local function MakeColorBtn(parent, label, y, cb)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.95, 0, 0, 24)
        btn.Position = UDim2.new(0.025, 0, y, 0)
        btn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
        btn.BorderSizePixel = 0
        btn.TextColor3 = Color3.fromRGB(220, 220, 230)
        btn.TextSize = 12
        btn.Font = Enum.Font.Gotham
        btn.Text = label
        btn.Parent = parent
        btn.MouseButton1Click:Connect(cb)
        return btn
    end
    
    local function UpdateContent(tabName)
        for _, child in ipairs(content:GetChildren()) do
            child:Destroy()
        end
        
        if tabName == "COMBAT" then
            MakeToggle(content, "AIMBOT", 0, function() return AIMon end, function(v) AIMon = v ToggleFOVCircle(AIMon) end)
            MakeToggle(content, "WALL CHECK", 0.08, function() return CheckWalls end, function(v) CheckWalls = v end)
            MakeToggle(content, "FOV CIRCLE", 0.16, function() return ShowFOVCircle end, function(v) ShowFOVCircle = v ToggleFOVCircle(AIMon and ShowFOVCircle) end)
            MakeSlider(content, "FOV", 0.26, 50, 500, function() return FOV end, function(v) FOV = v UpdateFOVCircle() end)
            MakeSlider(content, "SMOOTHNESS", 0.38, 1, 100, function() return math.floor(Smoothness * 100) end, function(v) Smoothness = v/100 end)
        elseif tabName == "VISUALS" then
            MakeToggle(content, "ESP", 0, function() return ESPon end, function(v)
                ESPon = v
                if not v then
                    for _, p in ipairs(Players:GetPlayers()) do if p.Character then RemoveESP(p.Character) end end
                    for _, c in ipairs(workspace:GetChildren()) do if c:IsA("Model") and c:FindFirstChild("Humanoid") then RemoveESP(c) end end
                else
                    for _, p in ipairs(Players:GetPlayers()) do if p.Character then CreateESP(p.Character) end end
                    for _, c in ipairs(workspace:GetChildren()) do if c:IsA("Model") and c:FindFirstChild("Humanoid") and c ~= LocalPlayer.Character then CreateESP(c) end end
                end
            end)
            MakeColorBtn(content, "CHANGE COLOR", 0.08, function()
                ESPcolor = Color3.new(math.random(), math.random(), math.random())
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Character then
                        local b = p.Character:FindFirstChild("BoxHandleAdornment")
                        if b then b.Color3 = ESPcolor end
                    end
                end
                for _, c in ipairs(workspace:GetChildren()) do
                    if c:IsA("Model") and c:FindFirstChild("Humanoid") then
                        local b = c:FindFirstChild("BoxHandleAdornment")
                        if b then b.Color3 = ESPcolor end
                    end
                end
            end)
            MakeSlider(content, "BOX SIZE", 0.18, 1, 10, function() return math.floor(BoxSize) end, function(v)
                BoxSize = v
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Character then
                        local b = p.Character:FindFirstChild("BoxHandleAdornment")
                        if b then b.Size = Vector3.new(BoxSize, BoxSize + 1.5, 1) end
                    end
                end
                for _, c in ipairs(workspace:GetChildren()) do
                    if c:IsA("Model") and c:FindFirstChild("Humanoid") then
                        local b = c:FindFirstChild("BoxHandleAdornment")
                        if b then b.Size = Vector3.new(BoxSize, BoxSize + 1.5, 1) end
                    end
                end
            end)
            MakeSlider(content, "TEXT SIZE", 0.30, 8, 24, function() return ESPsize end, function(v)
                ESPsize = v
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Character then
                        local bg = p.Character:FindFirstChild("BillboardGui")
                        if bg then
                            local lbl = bg:FindFirstChild("TextLabel")
                            if lbl then lbl.TextSize = v end
                        end
                    end
                end
            end)
        elseif tabName == "FUTURES" then
            MakeToggle(content, "FLY [F]", 0, function() return Fly end, function(v) ToggleFly() end)
            MakeToggle(content, "NOCLIP", 0.08, function() return NoClip end, function(v) ToggleNoClip() end)
            MakeSlider(content, "SPEED", 0.18, 1, 5, function() return Speed end, function(v) SetSpeed(v) end)
        end
    end
    
    local tabs = {"COMBAT", "VISUALS", "FUTURES"}
    local tabBtns = {}
    
    for i, name in ipairs(tabs) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.3, 0, 1, 0)
        btn.Position = UDim2.new((i-1) * 0.333, 0, 0, 0)
        btn.BackgroundTransparency = 1
        btn.Text = name
        btn.TextColor3 = Color3.fromRGB(150, 150, 170)
        btn.TextSize = 11
        btn.Font = Enum.Font.GothamBold
        btn.Parent = tb
        btn.Name = name
        tabBtns[name] = btn
        
        btn.MouseButton1Click:Connect(function()
            for _, b in pairs(tabBtns) do
                b.TextColor3 = Color3.fromRGB(150, 150, 170)
            end
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            UpdateContent(name)
        end)
    end
    
    if tabBtns["COMBAT"] then
        tabBtns["COMBAT"].TextColor3 = Color3.fromRGB(255, 255, 255)
    end
    
    UpdateContent("COMBAT")
    return gui
end

local function ScanAll()
    for _, p in ipairs(Players:GetPlayers()) do
        if p.Character and p.Character ~= LocalPlayer.Character then
            CreateESP(p.Character)
        end
    end
    for _, c in ipairs(workspace:GetChildren()) do
        if c:IsA("Model") and c:FindFirstChild("Humanoid") and c ~= LocalPlayer.Character then
            local isP = false
            for _, p in ipairs(Players:GetPlayers()) do
                if p.Character == c then isP = true break end
            end
            if not isP then CreateESP(c) end
        end
    end
end

-- ========== ЗАПУСК ==========

ShowLoadingScreen()
CreateMenu()
CreateFOVCircle()
ToggleFOVCircle(false)
CreateMobileButton()
ScanAll()

task.wait(0.5)
StartUpdateLoop()
StartFlyLoop()

LocalPlayer.CharacterAdded:Connect(function(char)
    task.wait(0.5)
    if scriptLoaded then
        StartUpdateLoop()
        StartFlyLoop()
    end
end)

Players.PlayerAdded:Connect(function(p)
    p.CharacterAdded:Connect(function(c)
        task.wait(0.5)
        if scriptLoaded and ESPon and c ~= LocalPlayer.Character then
            CreateESP(c)
        end
    end)
end)

workspace.ChildAdded:Connect(function(c)
    if scriptLoaded and ESPon and c:IsA("Model") and c:FindFirstChild("Humanoid") then
        local isP = false
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Character == c then isP = true break end
        end
        if not isP and c ~= LocalPlayer.Character then
            task.wait(0.3)
            CreateESP(c)
        end
    end
end)

workspace.ChildRemoved:Connect(function(c)
    if c:IsA("Model") then RemoveESP(c) end
end)

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if not scriptLoaded then return end
    
    if input.KeyCode == Enum.KeyCode.X then
        menuOpen = not menuOpen
        if gui then
            local m = gui:FindFirstChild("Main")
            if m then m.Visible = menuOpen end
        end
    end
    
    if input.KeyCode == Enum.KeyCode.Z then
        AIMon = not AIMon
        ToggleFOVCircle(AIMon and ShowFOVCircle)
    end
    
    if input.KeyCode == Enum.KeyCode.F then
        ToggleFly()
        if gui and gui:FindFirstChild("Main") and gui.Main:FindFirstChild("Content") then
            local content = gui.Main.Content
            for _, child in ipairs(content:GetChildren()) do
                if child:IsA("TextButton") then
                    local lbl = child:FindFirstChild("TextLabel")
                    if lbl and lbl.Text == "FLY [F]" then
                        local st = child:FindFirstChildOfClass("TextLabel")
                        if st and st.TextXAlignment == Enum.TextXAlignment.Right then
                            st.Text = Fly and "ON" or "OFF"
                            st.TextColor3 = Fly and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(255, 80, 80)
                        end
                    end
                end
            end
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if not scriptLoaded or not ESPon then return end
    for _, p in ipairs(Players:GetPlayers()) do
        local c = p.Character
        if c and c ~= LocalPlayer.Character and c:FindFirstChild("HumanoidRootPart") then
            if not c:FindFirstChild("BoxHandleAdornment") then
                CreateESP(c)
            end
        end
    end
    for _, c in ipairs(workspace:GetChildren()) do
        if c:IsA("Model") and c:FindFirstChild("Humanoid") and c ~= LocalPlayer.Character then
            local isP = false
            for _, p in ipairs(Players:GetPlayers()) do
                if p.Character == c then isP = true break end
            end
            if not isP and c:FindFirstChild("HumanoidRootPart") and not c:FindFirstChild("BoxHandleAdornment") then
                CreateESP(c)
            end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if not scriptLoaded or not AIMon then return end
    local target = GetClosest()
    if target then
        local root = target:FindFirstChild("HumanoidRootPart")
        if root then
            local pos = root.Position + Vector3.new(0, 1.5, 0)
            local current = Camera.CFrame.Position
            local look = (pos - current).Unit
            local newLook = Camera.CFrame.LookVector:Lerp(look, Smoothness)
            Camera.CFrame = CFrame.lookAt(current, current + newLook)
        end
    end
end)

print("ALL IN ONE loaded. X = Menu, Z = AIM, F = FLY")
